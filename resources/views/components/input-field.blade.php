<div class="input-field {{ $classes }}">
    <input class="input-field__input" type="{{ $type }}" name="{{ $name }}" />
    <span class="input-field__label">{{ $label }}</span>
    <span class="input-field__error error error_{{ $name }}"></span>
</div>